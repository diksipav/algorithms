// const maxSliceSum = (list) => {
//   let maxEnding = list[0];
//   let maxSlice = list[0];

//   for (let i = 1; i < list.length; i++) {
//     maxEnding = Math.max(list[i], maxEnding + list[i]);
//     maxSlice = Math.max(maxSlice, maxEnding);
//   }

//   return maxSlice;
// };

// // tests
// console.log(maxSliceSum([3, 2, -6, 4, 0]));
